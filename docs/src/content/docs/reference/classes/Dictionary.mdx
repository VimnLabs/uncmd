---
editUrl: false
next: false
prev: false
title: "Dictionary"
---

Defined in: [package/src/classes/dictionary.ts:24](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L24)

## Extends

- `Map`\<`K`, `V`\>

## Type Parameters

<table>
<thead>
<tr>
<th>Type Parameter</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`K`

</td>
</tr>
<tr>
<td>

`V`

</td>
</tr>
</tbody>
</table>

## Constructors

### new Dictionary()

```ts
new Dictionary<K, V>(entries?): Dictionary<K, V>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:50

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`entries`?

</td>
<td>

`null` \| readonly readonly \[`K`, `V`\][]

</td>
</tr>
</tbody>
</table>

#### Returns

[`Dictionary`](/uncmd/reference/classes/dictionary/)\<`K`, `V`\>

#### Inherited from

```ts
Map<K, V>.constructor
```

### new Dictionary()

```ts
new Dictionary<K, V>(iterable?): Dictionary<K, V>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:49

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`iterable`?

</td>
<td>

`null` \| `Iterable`\<readonly \[`K`, `V`\], `any`, `any`\>

</td>
</tr>
</tbody>
</table>

#### Returns

[`Dictionary`](/uncmd/reference/classes/dictionary/)\<`K`, `V`\>

#### Inherited from

```ts
Map<K, V>.constructor
```

## Properties

### size

```ts
readonly size: number;
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:45

#### Returns

the number of elements in the Map.

#### Inherited from

```ts
Map.size
```

***

### \[toStringTag\]

```ts
readonly [toStringTag]: string;
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.symbol.wellknown.d.ts:137

#### Inherited from

```ts
Map.[toStringTag]
```

***

### \[species\]

```ts
readonly static [species]: MapConstructor;
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.symbol.wellknown.d.ts:319

#### Inherited from

```ts
Map.[species]
```

## Methods

### clear()

```ts
clear(): void
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:20

#### Returns

`void`

#### Inherited from

```ts
Map.clear
```

***

### delete()

```ts
delete(key): boolean
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:24

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`key`

</td>
<td>

`K`

</td>
</tr>
</tbody>
</table>

#### Returns

`boolean`

true if an element in the Map existed and has been removed, or false if the element does not exist.

#### Inherited from

```ts
Map.delete
```

***

### forEach()

```ts
forEach(callbackfn, thisArg?): void
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:28

Executes a provided function once per each key/value pair in the Map, in insertion order.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callbackfn`

</td>
<td>

(`value`, `key`, `map`) => `void`

</td>
</tr>
<tr>
<td>

`thisArg`?

</td>
<td>

`any`

</td>
</tr>
</tbody>
</table>

#### Returns

`void`

#### Inherited from

```ts
Map.forEach
```

***

### get()

```ts
get(key): undefined | V
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:33

Returns a specified element from the Map object. If the value that is associated to the provided key is an object, then you will get a reference to that object and any change made to that object will effectively modify it inside the Map.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`key`

</td>
<td>

`K`

</td>
</tr>
</tbody>
</table>

#### Returns

`undefined` \| `V`

Returns the element associated with the specified key. If no element is associated with the specified key, undefined is returned.

#### Inherited from

```ts
Map.get
```

***

### has()

```ts
has(key): boolean
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:37

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`key`

</td>
<td>

`K`

</td>
</tr>
</tbody>
</table>

#### Returns

`boolean`

boolean indicating whether an element with the specified key exists or not.

#### Inherited from

```ts
Map.has
```

***

### set()

```ts
set(key, value): this
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.collection.d.ts:41

Adds a new element with a specified key and value to the Map. If an element with the same key already exists, the element will be updated.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`key`

</td>
<td>

`K`

</td>
</tr>
<tr>
<td>

`value`

</td>
<td>

`V`

</td>
</tr>
</tbody>
</table>

#### Returns

`this`

#### Inherited from

```ts
Map.set
```

***

### \[iterator\]()

```ts
iterator: MapIterator<[K, V]>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.iterable.d.ts:143

Returns an iterable of entries in the map.

#### Returns

`MapIterator`\<\[`K`, `V`\]\>

#### Inherited from

```ts
Map.[iterator]
```

***

### entries()

```ts
entries(): MapIterator<[K, V]>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.iterable.d.ts:148

Returns an iterable of key, value pairs for every entry in the map.

#### Returns

`MapIterator`\<\[`K`, `V`\]\>

#### Inherited from

```ts
Map.entries
```

***

### keys()

```ts
keys(): MapIterator<K>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.iterable.d.ts:153

Returns an iterable of keys in the map

#### Returns

`MapIterator`\<`K`\>

#### Inherited from

```ts
Map.keys
```

***

### values()

```ts
values(): MapIterator<V>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2015.iterable.d.ts:158

Returns an iterable of values in the map

#### Returns

`MapIterator`\<`V`\>

#### Inherited from

```ts
Map.values
```

***

### groupBy()

```ts
static groupBy<K, T>(items, keySelector): Map<K, T[]>
```

Defined in: docs/node\_modules/.pnpm/typescript@5.7.3/node\_modules/typescript/lib/lib.es2024.collection.d.ts:25

Groups members of an iterable according to the return value of the passed callback.

#### Type Parameters

<table>
<thead>
<tr>
<th>Type Parameter</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`K`

</td>
</tr>
<tr>
<td>

`T`

</td>
</tr>
</tbody>
</table>

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`items`

</td>
<td>

`Iterable`\<`T`\>

</td>
<td>

An iterable.

</td>
</tr>
<tr>
<td>

`keySelector`

</td>
<td>

(`item`, `index`) => `K`

</td>
<td>

A callback which will be invoked for each item in items.

</td>
</tr>
</tbody>
</table>

#### Returns

`Map`\<`K`, `T`[]\>

#### Inherited from

```ts
Map.groupBy
```

***

### filter()

```ts
filter(callback): Dictionary<K, V>
```

Defined in: [package/src/classes/dictionary.ts:31](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L31)

Filters the entries of the dictionary based on the provided callback.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

(`value`, `key`, `dict`) => `boolean`

</td>
<td>

A function to test each entry. Returns `true` to keep the entry, `false` otherwise.

</td>
</tr>
</tbody>
</table>

#### Returns

[`Dictionary`](/uncmd/reference/classes/dictionary/)\<`K`, `V`\>

A new `Dictionary` with the filtered entries.

***

### find()

```ts
find(callback): undefined | V
```

Defined in: [package/src/classes/dictionary.ts:45](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L45)

Finds the first value in the dictionary that satisfies the provided callback.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

(`value`, `key`, `dict`) => `boolean`

</td>
<td>

A function to test each entry. Returns `true` for the desired entry.

</td>
</tr>
</tbody>
</table>

#### Returns

`undefined` \| `V`

The first value that satisfies the callback, or `undefined` if none do.

***

### every()

```ts
every(callback): boolean
```

Defined in: [package/src/classes/dictionary.ts:63](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L63)

Tests whether all entries in the dictionary pass the provided callback.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

(`value`, `key`, `dict`) => `boolean`

</td>
<td>

A function to test each entry. Returns `true` for entries that pass.

</td>
</tr>
</tbody>
</table>

#### Returns

`boolean`

`true` if all entries pass the callback, otherwise `false`.

***

### some()

```ts
some(callback): boolean
```

Defined in: [package/src/classes/dictionary.ts:79](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L79)

Tests whether at least one entry in the dictionary passes the provided callback.

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

(`value`, `key`, `dict`) => `boolean`

</td>
<td>

A function to test each entry. Returns `true` for entries that pass.

</td>
</tr>
</tbody>
</table>

#### Returns

`boolean`

`true` if at least one entry passes the callback, otherwise `false`.

***

### reduce()

```ts
reduce<T>(callback, initial): T
```

Defined in: [package/src/classes/dictionary.ts:96](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L96)

Reduces the dictionary's entries to a single value using the provided callback.

#### Type Parameters

<table>
<thead>
<tr>
<th>Type Parameter</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
</tr>
</tbody>
</table>

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

(`accumulator`, `value`, `key`, `dict`) => `T`

</td>
<td>

A function to process each entry.

</td>
</tr>
<tr>
<td>

`initial`

</td>
<td>

`T`

</td>
<td>

The initial accumulator value.

</td>
</tr>
</tbody>
</table>

#### Returns

`T`

The result of the reduction.

***

### map()

```ts
map<T>(callback): Dictionary<K, T>
```

Defined in: [package/src/classes/dictionary.ts:112](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L112)

Maps the dictionary's entries to a new `Dictionary` with transformed values.

#### Type Parameters

<table>
<thead>
<tr>
<th>Type Parameter</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`T`

</td>
</tr>
</tbody>
</table>

#### Parameters

<table>
<thead>
<tr>
<th>Parameter</th>
<th>Type</th>
<th>Description</th>
</tr>
</thead>
<tbody>
<tr>
<td>

`callback`

</td>
<td>

(`value`, `key`, `dict`) => `T`

</td>
<td>

A function to transform each entry.

</td>
</tr>
</tbody>
</table>

#### Returns

[`Dictionary`](/uncmd/reference/classes/dictionary/)\<`K`, `T`\>

A new `Dictionary` with the mapped values.

***

### first()

```ts
first(): undefined | V
```

Defined in: [package/src/classes/dictionary.ts:125](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L125)

Retrieves the first value in the dictionary.

#### Returns

`undefined` \| `V`

The first value, or `undefined` if the dictionary is empty.

***

### last()

```ts
last(): undefined | V
```

Defined in: [package/src/classes/dictionary.ts:134](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L134)

Retrieves the last value in the dictionary.

#### Returns

`undefined` \| `V`

The last value, or `undefined` if the dictionary is empty.

***

### clone()

```ts
clone(): Dictionary<K, V>
```

Defined in: [package/src/classes/dictionary.ts:143](https://github.com/KingsBeCattz/uncmd/blob/bad6bae9d56c0bb79ede2176419790e96cc812b7/package/src/classes/dictionary.ts#L143)

Creates a shallow copy of the dictionary.

#### Returns

[`Dictionary`](/uncmd/reference/classes/dictionary/)\<`K`, `V`\>

A new `Dictionary` instance with the same entries, limit, and name.
